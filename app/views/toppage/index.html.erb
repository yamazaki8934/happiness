<head>
	<!-- ビューポートの設定 -->
	<meta name="viewport" content="width=device-width,initial-scale=1">
	<script src="https://code.jquery.com/jquery-3.2.1.min.js" type="text/javascript"></script>
</head>

<body>
  <header>
		<div class="container">
			<span class="fa fa-bars fa-2x menu-icon" id="bar">
			  <div class="header-right">
					<i class="fa fa-times-circle x-mark" id="mark"></i>
					<ul class="header-right1">
						<li class="my-account">
		          <%= link_to 'あなた宛のレビュー', receife_path(current_user.id), method: :get, class:'logout1', style:'color: black;' %>
		        </li>
						<li class="my-account">
		          <%= link_to 'アカウント情報の変更', edit_profile_path(current_user.id), method: :get, class:'logout1', style:'color: black;' %>
		        </li>
					  <li class="my-account">
					    <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class:'logout1', style:'color: black;' %>
					  </li>
					</ul>
				</div>
			</span>
			<!-- <span class="fa fa-bell fa-2x bell-icon" id="bell">
			  <div class="header-another">
					<% @activities.each do |activity| %>
					  <%= link_to activity.owner.nickname, activity.owner if activity.owner -%>
					  <%= link_to activity.trackable.nickname, activity.trackable -%>
          <% end %>
				</div>
			</span> -->
	  </div>
	</header>

  <div class="user-title-toppage" id="title">
    <span class="English">Happiness</span><span class="Japanese0">へ</span>
    <p class="Japanese">ようこそ！</p>
  </div>

	<div class="summary" id="summary">
    <p class="summary1">Happinessはユーザーが登録した自分の気になる性格、能力などについて友達から星マークでレビューを受けられるサービスです。（匿名です）</p>
		<p class="summary2">まずは「友達をレビューする」か「レビュー項目の登録」を選んでください。</p>
	</div>

  <div class="choose" id="button">
		<div class="choose-left">
			<div class="choose-button">
        <%= link_to "友達をレビューする", users_path, class:'choose-button10', style:'color: white;' %>
      </div>
		</div>
		<div class="choose-right">
      <div class="choose-button">
        <%= link_to "レビュー項目の登録", edit_user_path(current_user.id), class:'choose-button1', style:'color: white;' %>
      </div>
		</div>
	</div>

  <div class="my-review-show0">
		<div class="my-review-show1">
			友達からもらったレビューを見る場合は下の「レビューを見る」を選んでください。
		</div>
		<div class="choose-center">
      <div class="choose-center1">
        <%= link_to "レビューを見る", receife_path(current_user.id), class:'choose-button1', style:'color: white;' %>
      </div>
		</div>
	</div>

	<script type="text/javascript">
   $(function(){
      // ▼マウスが載ったらサブメニューを表示
      $("#bar").mouseover(function(){
				 $("#title").hide();
				 $("#summary").hide();
         $("#button").hide();
         $(this).find(".header-right").fadeIn();     // 自分のサブメニューを表示する。
      });
      // ▼どこかがクリックされたらサブメニューを消す
      $('html, #mark').click(function() {
				 $("#title").show(150);
				 $("#summary").show(150);
				 $("#button").show(150);
         $('.menu-icon').find(".header-right").slideUp(150);
      });
   });
  </script>



</body>
